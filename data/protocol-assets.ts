// protocol-assets.ts
// Map protocol IDs to asset paths in /assets
export const PROTOCOL_ASSETS: Record<string, string> = {
  // Example mapping — replace these IDs with real protocol ids from your data
  // 'anatomical-model-001': '/assets/digitaltwin.jpeg',
  // Add more mappings as you add images to the assets/ folder
};

export default PROTOCOL_ASSETS;
